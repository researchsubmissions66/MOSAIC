__pycache__/
*.pyc
*.pyo
*.log
.DS_Store
.ipynb_checkpoints/

# Local run artefacts, not part of the released study
slurm_logs/
results.backup-*/
# Cluster-specific batch scripts: they hardcode an account, venv and home dir,
# so they are local infrastructure rather than part of the released artefact.
scripts/mosaic_*.sh

# Stray render target: render_figures.py --out results/figures_png was used for a
# one-off PNG preview; results/figures is the canonical output.
results/figures_png/

# Fitted aligner objects: ~1 GB across the group trees, refittable from the
# committed tables by run_study.py. Never needed to reproduce a figure.
*.joblib
